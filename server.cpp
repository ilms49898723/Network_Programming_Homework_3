#include <bits/stdc++.h>
#include <cstring>
#include <cstdlib>
#include <thread>
#include "Socket.hpp"

int main(const int& argc, const char** argv) {
    if (argc != 2) {
        fprintf(stderr, "usage %s <port>\n", argv[0]);
        return EXIT_FAILURE;
    }
    return 0;
}
