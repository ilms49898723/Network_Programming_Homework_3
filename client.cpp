#include <bits/stdc++.h>
#include <cstring>
#include <cstdlib>
#include <thread>
#include "Socket.hpp"

int main(const int argc, const char** argv) {
    if (argc != 3) {
        fprintf(stderr, "usage %s <server address> <port>\n", argv[0]);
        return EXIT_FAILURE;
    }
    return 0;
}

